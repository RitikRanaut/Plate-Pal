import classes from "./MealItemForm.module.css";

const MeatItemForm =(prosp)=>{
    return (
        <form className={classes.form}>
            <input />
            <button>+ Add</button>
        </form>
    );
};

export default MeatItemForm;